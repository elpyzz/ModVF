{
  "name": "modvf",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "dev:api": "npm run dev -w @modvf/api",
    "build": "npm run build -w web",
    "build:api": "npm run build -w @modvf/api",
    "lint": "npm run lint -w web"
  }
}
